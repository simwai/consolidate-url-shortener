{
  "name": "server",
  "version": "1.0.0",
  "description": "consolidate url shortener backend",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "simwai",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
